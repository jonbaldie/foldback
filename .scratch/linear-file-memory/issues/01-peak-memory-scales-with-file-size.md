Status: ready-for-agent

# Peak memory grows linearly with the largest file processed (backup/verify/restore), despite documented streaming

Found by runtime-instrumented testing (RTS `-s` GC statistics), 2026-09-10 (foldback 0.1.0.0, commit 1c7348c, macOS, normal build; an `--ghc-options=-rtsopts` scratch build was used once for GC-sizing confirmation and deleted).
Evidence: `/tmp/foldback-exploratory-20260910/` (round-3 report: `report-round3.md`, replay logs `repro-mem-{1,2,3}.txt`, script `repro-linear-memory.sh`).

## User impact

README documents that "an individual file is streamed once into a temporary object" in 64 KiB chunks, but peak heap usage is proportional to the file size. A user backing up (or verifying/restoring) a 10 GB file can expect roughly 9–12 GB RSS — the opposite of streaming, with OOM/machine-pressure risk on typical laptops. Measured on the normal build (`+RTS -s`):

| file size | max residency (backup) | total memory in use |
|---|---|---|
| 50 MB | 37.8 MB | 63 MiB |
| 200 MB | 130–151 MB | 252 MiB |
| 400 MB | 302 MB | 494 MiB |
| 800 MB | 605 MB | 986 MiB |

~0.75× file size in peak live heap, ~1.2× in RSS — for backup, verify, and restore alike. With GC pressure forced (`+RTS -A64k` on a disclosed `-rtsopts` instrumentation build) residency is unchanged (151 MB at 200 MB): retention, not GC laziness.

## Replay (confirmed 3/3 on the normal build from a known starting state)

```sh
./repro-linear-memory.sh <foldback-binary> <scratch-dir> 200
# backup 200MB: ~130–151MB max residency, 252 MiB total in use
# verify:       ~168MB max residency
# restore:      ~185MB max residency (restore byte-identical every time)
```

Script body: create an N MB random file, `backup … +RTS -s | grep residency`, same for `verify` and `restore`.

## Expected vs actual

- Expected: memory bounded by chunk size (64 KiB) plus small state, independent of file size.
- Actual: peak residency and RSS linear in file size for every command that touches file contents.

## Mechanism hypothesis (for root-cause investigation)

The streaming loops pass the hash context as a plain argument without forcing it:

- `copyAndHash output (SHA256.update context chunk) (size + …) input` (`src/Foldback/Repository.hs:262`)
- `hashChunks (SHA256.update context chunk) input` (`src/Foldback/Repository.hs:284`)

Each iteration allocates an unevaluated `update` thunk whose closure references one 64 KiB chunk plus the previous thunk; nothing forces intermediate contexts until `finalize`, so every chunk stays live simultaneously. A strictness annotation on the accumulator per iteration (e.g. `seq`/bang on `context` and `size`) should restore constant-memory streaming — verify by re-running the reproducer.

## Comments

## Triage Notes

> *This was generated by AI during triage.*

Verified by reproduction (2026-09-11): an `-rtsopts` scratch build measured 151,158,472 bytes max residency verifying a 200 MB file — matching the reporter's 151 MB figure exactly.

Redundancy check: no forcing/strictness exists on the hash-context or byte-count accumulators in either streaming loop; no chunk-size or batching mechanism exists that could bound memory. No prior rejections in `.out-of-scope/`.

**Category:** bug. **State:** `ready-for-agent` — measured repro, grounded mechanism hypothesis, and clear fix direction; the exact forcing technique is within agent discretion.

### Agent Brief

**Category:** bug
**Summary:** Bound peak memory during backup/verify/restore to a constant (independent of file size) by forcing the streaming accumulators each iteration

**Current behavior:**
The streaming loops that hash and copy file content pass the hash context (and byte count) as plain, unforced accumulator arguments. Each iteration allocates an unevaluated update thunk whose closure retains its 64 KiB chunk plus the previous thunk; nothing forces the intermediate contexts until finalize, so all chunks stay live simultaneously. Peak heap and RSS grow linearly with the largest file processed (measured ~0.75× file size in residency, ~1.2× in RSS), contradicting the documented 64 KiB chunk streaming and risking OOM on large files.

**Desired behavior:**
Peak memory for every command that streams file content is bounded by a small constant (chunk size plus modest fixed state) regardless of file size. The hashes produced must be identical to the current implementation's (SHA-256 over the same bytes) — a strictness change only, no semantic change to digests, sizes, or output formats.

**Key interfaces:**
- The copy-and-hash streaming loop in the repository module (input handle → output object + SHA-256 context + running size): force both accumulators each iteration (e.g. strict fold / bang pattern / explicit seq) so no thunk chain accumulates.
- The hash-only streaming loop in the repository module (input handle → SHA-256 context): same forcing.
- Any other place file content flows through lazy accumulators during backup, verify, or restore.

**Acceptance criteria:**
- [ ] With `+RTS -s` (an `-rtsopts` build), max residency during `backup`, `verify`, and `restore` of a 200 MB file is a small constant, not proportional to file size (order of MiB, not hundreds of MiB)
- [ ] The same bound holds for a larger file (e.g. 800 MB): residency does not grow
- [ ] Backed-up object digests and restored files are byte-identical to the current implementation's output for the same input
- [ ] Existing tests pass; a regression test or documented measurement guards the bound

**Out of scope:**
- Changing the chunk size (64 KiB stays)
- Parallel or multi-threaded hashing
- Changing the repository or object format